import { createElement } from './helpers.js';

const bubbleLg = createElement('span', { class: 'bubble bubble__lg' });
const bubbleSm = createElement('span', { class: 'bubble bubble__sm' });

export { bubbleLg, bubbleSm }